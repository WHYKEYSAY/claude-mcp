# Linux 配额优化系统 - 快速使用指南

## 🚀 一键安装和启动

### 步骤 1: 安装
```bash
cd /mnt/c/Users/whyke/github/claude-mcp/linux
bash setup-linux.sh
```

### 步骤 2: 重新加载 Shell
```bash
source ~/.bashrc
```

### 步骤 3: 启动！
```bash
claude-opt
```

或使用更短的命令：
```bash
cq
```

## 📋 安装后的文件位置

安装脚本会创建以下文件：

```
~/.local/bin/
  └── claude-optimized.sh          # 主启动脚本

~/.claude/
  └── system-prompt.md             # 系统提示配置

~/
  └── QUICK-REFERENCE.txt          # 快速参考卡片

~/.bashrc (或 ~/.zshrc)
  └── claude-opt 别名              # 快捷命令
  └── cq 别名                      # 更短的快捷命令
```

## 💬 使用方式

### 方式 1: 使用别名 (推荐) ⭐
```bash
claude-opt
```

或：
```bash
cq
```

### 方式 2: 完整路径
```bash
~/.local/bin/claude-optimized.sh
```

### 方式 3: 如果 ~/.local/bin 在 PATH 中
```bash
claude-optimized.sh
```

## 🎯 启动后你会看到

```
🚀 启动 Claude Code 配额优化模式...

╔════════════════════════════════════════════════════════════╗
║     Claude Code Quota-Optimized Mode (v1.3)                ║
╚════════════════════════════════════════════════════════════╝

📋 核心原则:
   🔥 烧Gemini的免费配额 (1,500/天)
     ↓
   🛡️ 保护Claude的上下文 (200k)
     ↓
   ⏱️ 合理使用Codex的配额 (5小时制)

👥 角色分配:
   🏗️  Claude    → 文件操作、代码编写
   🔍 Gemini   → 信息查询、错误分析 (@gemini)
   🔬 Codex    → 逻辑审查、安全审计 (@codex)

🔄 工作流: Research (Gemini) → Implementation (Claude) → Verification (Codex)

✅ 检查MCP服务器状态...
   ✓ gemini: /home/keke/.local/bin/gemini-mcp-rs - ✓ Connected
   ✓ codex: /home/keke/.local/bin/codex-mcp-rs - ✓ Connected

🟢 模式已激活，输入你的任务:
```

## 💡 快速示例

### 查询文档 (用 Gemini)
```bash
@gemini search for Python asyncio best practices
```

### 创建代码 (用 Claude)
```bash
Create a FastAPI application with CORS support
```

### 审查代码 (用 Codex)
```bash
@codex review this function for security issues
```

### 完整工作流 (3-Step)
```bash
# Step A: 研究
@gemini find React Server Components documentation

# Step B: 实现
Create a React Server Component for user profile

# Step C: 审查
@codex review for performance and accessibility
```

## 🛠️ 故障排除

### 问题 1: 别名不工作
```bash
# 解决方案：重新加载配置
source ~/.bashrc

# 或者重启终端
```

### 问题 2: MCP 服务器未连接
```bash
# 检查状态
claude mcp list

# 如果显示 ✗ Failed，需要重新配置 MCP 服务器
```

### 问题 3: 找不到 claude 命令
```bash
# 安装 Claude Code
npm install -g @anthropic/claude-code

# 或检查是否在 PATH 中
which claude
```

### 问题 4: 权限问题
```bash
# 确保脚本可执行
chmod +x ~/.local/bin/claude-optimized.sh
```

## 📊 配额使用建议

### 🔥 Gemini (免费，大量使用)
- **何时用**: 查文档、搜索错误、学习最佳实践
- **配额**: ~1,500 请求/天
- **策略**: 不要节约，尽管用！

### 🛡️ Claude (昂贵，保护使用)
- **何时用**: 读写文件、编辑代码、执行命令
- **配额**: 200k tokens 上下文
- **策略**: 仅用于必须文件操作的任务

### ⏱️ Codex (有限，谨慎使用)
- **何时用**: 代码审查、安全检查、逻辑验证
- **配额**: 300-1500 消息/5小时
- **策略**: 保留给关键任务

## 🎓 学习资源

1. **QUICK-REFERENCE.txt** - 放在 `~/` 目录，随时查看
2. **README-LINUX.md** - 完整的 Linux 使用说明
3. **system-prompt.md** - 查看 Claude 的行为规则

## ✅ 验证清单

安装成功的标志：

- [ ] 运行 `claude-opt` 有响应
- [ ] 看到欢迎界面
- [ ] MCP 服务器显示 "✓ Connected"
- [ ] `alias | grep claude-opt` 有输出
- [ ] `ls ~/.local/bin/claude-optimized.sh` 文件存在
- [ ] `cat ~/.claude/system-prompt.md` 文件存在

## 🎉 开始使用

现在就试试：

```bash
# 1. 启动优化模式
claude-opt

# 2. 试一个查询
@gemini search for the latest TypeScript features

# 3. 创建一些代码
Create a simple TypeScript utility function

# 4. 审查代码
@codex review for potential type errors
```

## 📞 需要帮助？

- 快速参考: `cat ~/QUICK-REFERENCE.txt`
- 详细文档: `cat /mnt/c/Users/whyke/github/claude-mcp/linux/README-LINUX.md`
- MCP 状态: `claude mcp list`
- 检查别名: `alias | grep claude`

---

**版本**: v1.3
**创建日期**: 2025-12-10
**状态**: 🟢 就绪

🚀 **享受配额优化的开发体验！**
